#Append all IP addresses running dump1090/piaware in the next line, 127.0.0.1 is just an example you should replace it
SOURCES= 127.0.0.1 127.0.0.1

#for the IP addresses in SOURCES the ports 30005 and 30105 are used, if you want to only redirect a single or non-standard port use the following:
#uncomment and add IP addresses with ports for redirection
#CUSTOM=127.0.0.1:29005 127.0.0.1:29999

#you should't need to change the rest of this file
TARGET=127.0.0.1:29004

#options for dump1090-fa where all the data goes
RECEIVER_OPTIONS="--net-only"
DECODER_OPTIONS="--max-range 360"
NET_OPTIONS="--net --net-heartbeat 60 --net-ro-size 1000 --net-ro-interval 1 --net-ri-port 0 --net-ro-port 0 --net-sbs-port 0 --net-bi-port 29004 --net-bo-port 0"
JSON_OPTIONS="--json-location-accuracy 1"
